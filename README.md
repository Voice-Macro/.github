# .github
Voice Macro maps a short spoken command to one action on the Windows PC you already use for lab work. VoiceMacro keeps those commands in a single list, so a repeated step starts from your own microphone instead of a long path through menus you already know on that same host.
